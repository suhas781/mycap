# Frontend build-time env (copy to .env.local for local dev; set in host for production)
# Backend API URL – required in production if frontend and backend are on different domains
# VITE_API_URL=https://your-backend.railway.app
